# file: ansible-role-rtags/defaults/main.yml

rtags_repo_url: https://github.com/Andersbakken/rtags.git
rtags_version: v2.10
rtags_tmp: "/tmp/rtags"
rtags_build_dir: /tmp/rtags_build_dir
rtags_install_dir: "/opt/rtags-{{rtags_version}}"
bear_repo_url: https://github.com/rizsotto/Bear.git
bear_version: 2.3.5
bear_tmp: "/tmp/bear"
bear_build_dir: /tmp/bear_build_dir
bear_install_dir: "/opt/bear-{{bear_version}}"
llvm_version: 4.0
llvm_install_dir: "/opt/llvm-{{llvm_version}}"
gcc_version: 4.9.4
gcc_install_dir: "/opt/gcc-{{gcc_version}}"
