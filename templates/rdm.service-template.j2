[Unit]
Description="RTAGs daemon"
Requires=rdm.socket

[Service]
Type=simple
ExecStart={{rtags_install_dir}}/bin/rdm.sh -v --inactivity-timeout 300 --log-flush
ExecStartPost=/bin/sh -c "echo +19 > /proc/$MAINPID/autogroup"
Nice=19
CPUSchedulingPolicy=idle
